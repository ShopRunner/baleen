dependencies {
    compile project(':baleen')
    compile "org.apache.avro:avro:1.8.1"
    compile 'com.squareup:kotlinpoet:1.0.0'
    testCompile "org.jetbrains.kotlin:kotlin-compiler"
}

test {
    def map = [GEN_CLASSPATH: configurations.testCompile.files.path.join(":")]
    environment map
}